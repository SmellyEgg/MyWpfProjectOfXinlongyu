<UserControl x:Class="xinlongyuOfWpf.CustomControls.xinlongyuMultipleFileUploader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:xinlongyuOfWpf.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ListBox Margin="6">
            
        </ListBox>

        <StackPanel Orientation="Horizontal" Grid.Row="1"
                    HorizontalAlignment="Center">
            <Button Content="插入" Width="100" Height="30" Style="{DynamicResource ButtonWithRoundedCorner}" Background="#0080C0" Foreground="White" Margin="6"/>
            <Button Content="删除"  Width="100" Height="30" Style="{DynamicResource ButtonWithRoundedCorner}" Background="#0080C0" Foreground="White" Margin="6"/>
        </StackPanel>
        
    </Grid>
</UserControl>
